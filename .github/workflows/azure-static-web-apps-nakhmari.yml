name: Azure Static Web Apps CI/CD - Nakhmari

on:
  push:
    branches:
      - release/nakhmari

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy Nakhmari
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PRODUCTION }}
          action: "upload"
          app_location: "./frontend"
          api_location: "./functions"
          output_location: "/dist/app/browser"
          production_branch: "release/nakhmari"
