// Модуль объекта справочника СервисПроверкиСкидокПараметры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)

	ДополнитьСоответствиеПунктовАнкетыИзМакета();

КонецПроцедуры

Процедура ПередЗаписью(Отказ)

	ДополнитьСоответствиеПунктовАнкетыИзМакета();

КонецПроцедуры

Процедура ДополнитьСоответствиеПунктовАнкетыИзМакета() Экспорт

	МакетИменПолей =
		Справочники.СервисПроверкиСкидокПараметры.ПолучитьМакет(
			"ИменаПолей"
		);

	Для НомерСтроки = 1 По МакетИменПолей.ВысотаТаблицы Цикл

		ИмяПоля = МакетИменПолей.Область(НомерСтроки, 1).Текст;
		Отбор = Новый Структура("ИмяПунктаВСервисе", ИмяПоля);

		Если СоответствиеПунктовАнкеты.НайтиСтроки(Отбор).Количество() = 0 Тогда
			НоваяСтрока = СоответствиеПунктовАнкеты.Добавить();
			НоваяСтрока.ИмяПунктаВСервисе = ИмяПоля;
		КонецЕсли;

	КонецЦикла;

КонецПроцедуры

